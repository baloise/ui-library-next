{
  "name": "root",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "build": "npm run lib:build && npm run lib:vue:build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish": "lerna publish --yes",
    "lib:build": "npm --prefix packages/library run build",
    "lib:serve": "npm --prefix packages/library run serve",
    "lib:serve:ie": "npm --prefix packages/library run serve:ie",
    "lib:vue:clean": "rimraf packages/library-vue/node_modules",
    "lib:vue:build": "npm --prefix packages/library-vue run build",
    "testing:run": "npm --prefix packages/testing run ci",
    "testing:serve": "npm --prefix packages/testing run serve",
    "testing:server": "npm --prefix packages/testing run start-server",
    "vue:serve": "npm --prefix packages/examples/vue run serve"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "dependencies": {
    "cypress": "^5.2.0"
  }
}
